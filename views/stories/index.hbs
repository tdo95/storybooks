<h1>Stories</h1>
{{!-- references logged in user --}}
<div class="row">
    {{#each stories}}
        <div class="col s12 m4">
            <div class="card">
                <div class="card-image">
                    {{!-- TODO: edit icon to display if story belongs to user logged in --}}
                    {{!-- ../ goes up one level to grab logged in user, since we are in the stories loop, user refer's to the creator of the story -- jk this actually doesnt work on it's own lol, we have to set a gloabl variable for user --}}
                    {{!-- Also, note parsing HTML with handlebars requires a triple curlybrace --}}
                    {{{editIcon user ../user _id}}}
                </div>
                <div class="card-content center-align">
                    <h5>{{title}}</h5>
                    <p>{{stripTags (truncate body 150)}}</p>
                    <br>
                    <div class="chip">
                        <img src="{{user.image}}" alt="User image" referrerpolicy="no-referrer">
                        <a href="/stories/user/{{user._id}}">{{user.displayName}}</a>
                    </div>
                </div>
                <div class="card-action center-align">
                    <a href="/stories/{{_id}}" class="btn grey">Read More</a>
                </div>
            </div>
        </div>
    {{else}}
        <p>No stories to display</p>
    {{/each}}
</div>